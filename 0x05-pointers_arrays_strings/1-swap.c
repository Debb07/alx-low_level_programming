#include "main.h"

/**
 * swap_int - swaps the value of two integers
 *
 * @a - first parameter
 * @b - second parameter
 *
 * Description: swaps two numbers
 *
 * Return: Nothing
 */

void swap_int(int *a, int *b)
{
	int swap(c, d)
	{

	int c = &a;
	int d = &b;

	swap(d, c);
	}
}

